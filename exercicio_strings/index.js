// concatene duas palavres 
function concateneStrings(palavra1, palavra2) {
    
}

// modifique uma letra na palavra ex. 'a' para 'o' em qualquer palavra
const mudandoLetras = (palavra) => {
   
}

// verifique se a palavra começa com a letra 'P'
function verificaSePalavraComeca(Letra) {

}

// verifique se a palavra é um polimetro
// um polimetro é uma palavra que pode ser lida da mesma forma de trás para frente
// ex. arara, ovo, etc
// faça verificações para saber se a palavra é vazia, se é uma string, se é um número
// a palavra tem que esta em caixa baixa.
const verifiqueSePolimetro = (palavra) => {

}

// verifique quantas vogais tem na palavra
// faça verificações para saber se a palavra é vazia, se é uma string, se é um número
// a palavra tem que esta em caixa baixa.
function verifiqueVogais(palavra) {
 
}

module.exports = {
    concateneStrings,
    mudandoLetras,
    verificaSePalavraComeca,
    verifiqueSePolimetro,
    verifiqueVogais
}